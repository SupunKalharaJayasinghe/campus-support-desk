"use client";

import { Card } from "@/components/ui/Card";

export function ResourceList() {
  return (
    <Card title="ResourceList">
      <p className="text-sm text-slate-600">Placeholder component for ResourceList.</p>
    </Card>
  );
}
