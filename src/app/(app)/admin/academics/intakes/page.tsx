import PlaceholderPage from "@/components/admin/PlaceholderPage";

export default function IntakesPage() {
  return (
    <PlaceholderPage
      description="Manage intakes, batches, and cohort-level settings used across modules and assessments."
      highlights={[
        "Create intake windows (e.g., 2026 June)",
        "Batch-level capacity and registration rules",
        "Bulk move students between intakes",
      ]}
      status="In Progress"
      title="Intakes / Batches"
    />
  );
}

