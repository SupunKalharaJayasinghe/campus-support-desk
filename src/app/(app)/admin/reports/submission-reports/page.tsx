import PlaceholderPage from "@/components/admin/PlaceholderPage";

export default function SubmissionReportsPage() {
  return (
    <PlaceholderPage
      description="Submission reporting across modules, streams, and subgroups."
      highlights={[
        "Late submission distribution",
        "Submission anomalies and integrity flags",
        "Export packs for boards",
      ]}
      status="Planned"
      title="Submission Reports"
    />
  );
}

