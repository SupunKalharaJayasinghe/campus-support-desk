import PlaceholderPage from "@/components/admin/PlaceholderPage";

export default function SubgroupDeadlinesPage() {
  return (
    <PlaceholderPage
      description="Override deadlines per subgroup (labs/streams) while keeping a single assignment definition."
      highlights={[
        "Deadline exceptions with reasons",
        "Late submission windows",
        "Audit trail per change",
      ]}
      status="Planned"
      title="Subgroup Deadlines"
    />
  );
}

