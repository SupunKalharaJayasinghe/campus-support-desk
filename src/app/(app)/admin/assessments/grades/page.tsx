import PlaceholderPage from "@/components/admin/PlaceholderPage";

export default function GradesPage() {
  return (
    <PlaceholderPage
      description="Manage gradebooks, publish results, and export for academic board review."
      highlights={[
        "Grade lock + release workflow",
        "Export to CSV/PDF",
        "Moderation and recheck pipeline",
      ]}
      status="Planned"
      title="Grades"
    />
  );
}

